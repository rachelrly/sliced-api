function toMetric(amount_str) {
    //if amount doesnt start with number
    //return null form metric_unit
    //return null for amount

    //if amount starts with number
    //if second word.toLowerCase = [c, tbsp, tsp, cup, ml, l]
    //preform CALC
    //if second word.toLowerCase = anything else
    //return number as amount_in_metric
    //return null for metric_unit






}